# NNN · <Nombre> — Plan

_Cómo se implementa lo descrito en `spec.md`. Debe respetar la `constitution/`._

<!-- GUÍA: se escribe DESPUÉS de cerrar spec.md.
El plan describe el ESTADO ACTUAL, no su historia: si una decisión se revierte a
mitad de implementación, no la narres aquí — crea un ADR y enlázalo. -->

## Enfoque

<La estrategia en 3-5 frases: por qué esta y no la obvia. Qué responsabilidad
tiene este módulo y cuál deliberadamente no.>

## Implementación

1. `<ruta/modulo.py>` — <qué hace>.
2. `<ruta/modulo.py>` — <qué hace>.
3. `<ruta/models.py>` — <estructuras de datos de entrada/salida>.

## Decisiones

<!-- Una línea por decisión. Si tuvo alternativas con peso real, extráela a un ADR
y deja aquí solo el enlace: el plan no es un registro histórico. -->

- **<Elección> en vez de <alternativa>** — <motivo en una línea>.
  Ver [`ADR NNNN`](../../decisions/NNNN-<slug>.md).
- **<Convención local sin alternativas>** — <motivo>.

## Riesgos

- **<Riesgo concreto>** — se mitiga <cómo>. <Qué pasa si igual ocurre.>
